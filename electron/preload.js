// Intentionally empty: the window just loads the app's own pages, which
// need no Node/Electron APIs exposed to them. Kept as a real file (rather
// than omitting webPreferences.preload) so contextIsolation's default
// sandboxing is explicit, not incidental.
