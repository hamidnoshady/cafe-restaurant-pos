# Cafe/Restaurant POS — Phase Index

Each phase is a self-contained file with its own scope, exit criteria, and open questions. Work through them in order; don't start a phase until the previous one meets its exit criteria.

| Phase | File | Status |
|---|---|---|
| 0 | Phase-0-Foundation.md | Implemented |
| 1 | Phase-1-Setup-Wizard.md | Implemented |
| 2 | Phase-2-Menu-Cashier-Order-Flow.md | Implemented |
| 3 | Phase-3-Table-Management-Reservations.md | Implemented |
| 4 | Phase-4-Waiter-Kitchen-RealTime-Sync.md | Implemented |
| 5 | Phase-5-Offline-Queue-Hardware.md | Implemented |
| 6 | Phase-6-Inventory.md | Implemented |
| 7 | Phase-7-Double-Entry-Ledger.md | Implemented |
| 8 | Phase-8-Reporting-Analytics-Engine.md | Implemented |
| 9 | Phase-9-Multi-Location-Rollup-Polish.md | Implemented |
| 10 | Phase-10-Backup-System.md | Implemented |
| 11 | Phase-11-Delivery-Post-V1.md | Implemented |
| 12 | Phase-12-Multi-Business-Tenancy.md | Implemented |
| 13 | Phase-13-Teams-Permissions.md | Implemented |
| 14 | Phase-14-Multi-Location-Per-Business.md | Implemented |
| 15 | Phase-15-Super-Admin-Console.md | Implemented |
| 16 | Phase-16-Accounting-Suite.md | Implemented |
| 17 | Phase-17-Feature-Gating-Hardening.md | Implemented |
| 18 | Phase-18-AI-Platform-Administration-Credit-Billing.md | Implemented |
| 18b | Phase-18b-AI-Agent-Capability-Expansion.md | Complete for the documented existing-model scope (all five waves shipped; schema-dependent follow-ups deferred) |
| 19 | Phase-19-Public-API-Webhooks.md | In progress — Wave 2 core data API |
| 20 | Phase-20-Employee-Secure-Identity.md | Complete — all eight waves shipped |
| 21 | Phase-21-Multi-Industry-Accounting-Platform.md | Complete — all seven waves shipped. Jewelry (weight/purity, gold pricing, stones, consignment), watch (serialized units, warranty, repair tickets) and accessories (variant stock and pricing) each have their own item model, posting rules, chart of accounts, setup-wizard path and dashboard, all sharing Wave 1's domain-event posting engine; Wave 7 adds weight reconciliation, consignor statements and payouts, warranty/repair reporting, variant sales analysis and a per-item audit trail. Remaining open items are product decisions, not unfinished builds: an external gold-price feed, weight-based FIFO costing for bulk gold, and coin (per-unit) pricing |
| 22 | Phase-22-Accounting-Standards-Compliance-Gap-Analysis.md | Waves 1-5 and 7 complete (Wave 4 first slice; Wave 6 is Phase 21's own remaining waves, tracked there) — audit/gap-analysis, account hierarchy levels/nature/contra metadata, a terminology standards audit, revenue split by sales channel + a platform-commission expense account, a per-account دفتر معین/گردش حساب statement, a fixed-asset register with straight-line depreciation, and a full regression pass, against GitHub issue #160. Remaining scope (tip capture, food-cost variance report, an online-platform concept) needs product decisions before further work |
| 23 | Phase-23-Subdomain-Tenancy.md | Waves 1-6 complete — typable checksummed sync tokens, a `central`/`site` deployment role with a derived sync URL, and per-business subdomains, now cut over: `ROOT_DOMAIN` (which may itself be a subdomain, e.g. `biz1.ac.eshobe.com`) is the switch, the `/{slug}/dashboard` path scheme is deleted, and each business's subdomain is typed in English by a super-admin. Wave 6 makes it work behind a managed platform that rewrites `Host` (`TRUST_FORWARDED_HOST`). Still needs a wildcard certificate for `*.$ROOT_DOMAIN` in production. Filed as 23 because the issue's own "Phase 21" number was already taken |
| 23b | Phase-23-WooCommerce-Integration.md | Waves 1-6 complete — WooCommerce integration against GitHub issue #118: a stored, encrypted store connection; inbound orders posted financially; product and customer sync with remote↔local mapping; stock and price push through an outbox; refunds, reconciliation and error management; and an admin panel with monitoring. Filed as 23b because its own doc claims "Phase 23", which the subdomain-tenancy phase had already taken — the two are unrelated and shipped in parallel |
| 24 | Phase-24-Security-Hardening-Data-Protection.md | Designed — all five waves specified, implementation not started, against GitHub issue #228. A threat model (three trust boundaries, seven adversaries) plus: Wave 1 perimeter and credential hardening (security response headers with a nonce CSP, login lockout for the password realms, the shared `REMOTE_SYNC_TOKEN` denied by default, per-realm JWT keys, encrypted local/USB backups, a non-root container, `/ws` revocation re-checks); Wave 2 mandatory two-factor auth for platform admins and Owners (Kavenegar SMS OTP by default, Google Authenticator TOTP as the alternative, enrolled at business creation, grace period for existing accounts); Wave 3 field-level encryption at rest with per-business keys; Wave 4 VPN-only networking and LAN HTTPS; Wave 5 the remaining hardening. Zero-knowledge end-to-end encryption is explicitly rejected — it is incompatible with server-side reporting, the posting engine and the RLS predicates |
| 25 | Phase-25-Industry-Separation.md | Waves 1-4 complete — the business type becomes a real product boundary rather than a column Phase 21 only ever added to. A super-admin picks the industry when provisioning and can change it afterwards (additively and audited); `src/lib/industry-profile.ts` decides which modules a trade has, what they are called and how it sells, enforced at the API guard the way `features.ts` is; and the retail industries get a multi-line invoice on `orders`/`order_items`, settled through Phase 21's own sell services so no posting rule changes. Filed as 25 because 24 was taken by the security phase; it does not depend on it |
| 26 | Phase-26-Holoo-Interoperability.md | In progress — Holoo (هلو) as a second provider on the Phase 23 integration gateway, against GitHub issue #125 with waves #243–#252. Two needs on one version-aware adapter: full one-time migration (goods, persons, chart of accounts, opening balances, transactions, accounting vouchers with a trial-balance tie-out, and a migration wizard with preview, discrepancy report and per-run rollback) and companion mode («چهره‌ی هلو») — the app's POS, dashboard, reports and assistant on top of Holoo's own data while Holoo stays the official books, with a read-only mirror, a `409 holoo_owned` ownership guard in `withTenantScope`, a shadow-book time boundary on the connection, guarded web-service-first writes into Holoo, and nightly reconciliation + monitoring. Ships as migration `0103` (the issue's predicted `0072` was taken by later phases) |
| 27 | Phase-27-Cosmetics-And-Per-Trade-Specialization.md | Complete — all thirteen waves shipped. A fifth business type, «آرایشی و بهداشتی» (cosmetics & toiletries), reusing Phase 21's retail item model with a new batch/expiry layer (`item_batches`) and FEFO selling; and a specialized identity for each of the five trades, with everything two or more of them share built exactly once and switched on from `industry-profile.ts` — barcodes and label printing, loyalty and store credit, one promotion engine shared by F&B orders and retail invoices, sales-staff commission, and purchasing/returns/transfers finally reaching the `items` model. Then a flagship per trade: gram-denominated layaway, gold buy-back and حساب طلایی for jewelry; service-reminder CRM, pre-owned intake and printed repair estimates for watch; merchandising analytics for accessories and cosmetics; happy-hour pricing, recipe cost-drift alerts and waste analytics for the café; and a Wave 13 close-out report pack plus three new assistant read tools (near-expiry, commission, repurchase). Filed as 27 because 26 is claimed by the Holoo-interoperability issues; it does not depend on them |
| 28 | Phase-28-Connections-Hub-And-WordPress-Plugin.md | Implemented — one subject: everything the product connects to, and the fact that only one of those things had a screen. Starts from a reported failure (desktop pairing rejected the credential the cloud dashboard actually offered, because the only generator an owner could reach minted a server-sync token instead of a pairing code, and the address was a hardcoded constant rather than the origin they were signed in at) and generalises: owner self-service pairing with the request's own host as the address, a host-neutral redeem URL, an address test before the one-time code is spent, then `/dashboard/connections` carrying all three connection kinds — desktop install, WooCommerce store, developer API keys (Phase 19's missing screen) — each tab gated on its own entitlement. Adds a second WooCommerce link mode: a WordPress plugin holding one integration-scoped token, signing every request with a timestamp/nonce/body HMAC, pushing events and pulling jobs, so the app never dials the store. **Historical note:** Phase 40 moves WooCommerce out of this generic hub into the standalone WP Manager; old WooCommerce URLs redirect there. Filed as 28 because 26 is claimed by the Holoo-interoperability issues and 27 by the cosmetics phase |
| 29 | Phase-29-In-House-Production.md | Implemented — the F&B recipe model was one level deep, so anything a café *makes* (a cake built once from raw materials, then sold by the slice with its own serving recipe) could not be expressed: put the cake's materials in the per-slice recipe and every sale deducts a whole cake, leave them out and the cake has no cost. Adds a production formula and a production run between the two. The load-bearing decision is that the produced good is an ordinary `inventory_items` row flagged `is_produced` rather than a parallel model, so serving recipes, FIFO/weighted-average costing, sale-time deduction, stock counts, waste, suggested pricing and cost drift all keep working with no change at all. Inputs leave through the same exact path a sale uses and the output arrives through the purchase-receipt shape (settling any shortage of the produced item first); the transformation posts three entries through a WIP wash account, with labour absorbed as a contra-expense so it is not booked twice. Runs are reversible, refused once the batch has been sold. Lives as a «تولید» tab inside `/accounting/inventory`, so it inherits the `inventory` flag and F&B module with no new gating |
| 30 | Phase-30-Missing-Account-Headings-And-Cheques.md | Implemented — the chart of accounts, looked at whole for the first time since Phase 7. Each industry's seed chart had only ever been extended by the phase that needed an account, so the four retail templates never picked up what a *cross-industry* feature added: `ledger` is a core module for every trade, but `5200`, `1510`, `5700`, `5170` and `1210` were only in F&B's chart, so payroll, month-end depreciation, a markdown and a supplier-receivable return all failed with `ledger_account_missing` in a jewellery, watch, accessories or cosmetics shop. Shared account blocks now spread into all five templates so the invariant holds by construction, with an additive backfill for existing businesses, and `coa-template.test.ts` asserts one `CORE_REQUIRED_CODES` list across all five — the guard that was missing. Adds the headings no chart had (تخفیفات فروش، حق سرویس، کارمزد بانکی، کسری و اضافه صندوق، تنخواه، بیمه و مالیات حقوق پرداختنی، مالیات عملکرد، تسهیلات، برداشت مالک، سود/زیان فروش دارایی ثابت) and makes cost of sales industry-aware — a flat F&B list meant gross profit equalled total revenue for three of the five trades — plus a جاری/غیرجاری split on the balance sheet. Then cheques (چک), which the system had no representation of at all: `1240`/`2120` with a معین per place a cheque can be, a `cheques` + `cheque_events` subledger, and the full life including ظهرنویسی. The load-bearing decision is that `endorsed` is a *status*, not an account — an endorsed cheque is contingent, so the endorsement moves the supplier's balance for real and a bounce is a real entry that brings both back, rather than an unbalanced memo pair. The second is that a cheque is recorded in the register, not taken as a till tender: a tender slot cannot hold a serial, bank and due date, and a shift reconciles `gross_total` against exactly four method buckets |
| 31 | Phase-31-AI-Autopilot.md | Implemented — the assistant could see a problem at 08:00 and still needed an owner to open a chat window and click, because every "apply" in the product is a browser fetch carrying the signed-in user's session cookie: the Wave 4 tick has no request, no session and no cookie, so it could not act at all. Adds a server-side executor per eligible action that calls the *same service function the route handler calls* — never an HTTP loopback, never a second mutation path — and lets an owner decide in advance, per category, which class of change they have already confirmed. Five categories, eight of the thirteen actions eligible, each with owner-set caps inside a hard server ceiling that is clamped on write and again on read. The load-bearing decisions are the two boundaries autopilot does **not** cross, because neither is a confirm-loop convenience: a journal entry is only ever *drafted* into Phase 16's human approval queue (that queue exists so no single actor both writes and posts an entry — true whether a human or the agent drafted it), and nothing customer-facing is ever sent, only internal notes written. Waste is detected and flagged but never logged, because *why* stock left is a fact only a person in the room has. Over-cap never means dropped or forced — the proposal stays clickable on the identical manual path, and the values caps are measured against are read from the database, never taken from the model's own payload. Prior state is captured inside the write's own transaction, so undo restores what was actually there. Rides the existing proactive tick rather than opening a second tenant-enumeration bypass |

| 32 | Phase-32-AI-Coworker.md | Implemented — the assistant could answer a question and could act on its own judgement, but could not take an instruction and *keep* it: an owner who already knew the job («هر شب که شیفت بسته می‌شود، ماندهٔ نان را ضایعات بزن») had nowhere to put that sentence. Adds a **job** — a template, the owner's intent, a trigger (a business event, a time, or by hand) and an approval mode — that fires into an approval inbox. The load-bearing decision is that a job is **deterministic**: its actions are built by a pure function from parameters and database facts, with no provider call, so it costs no credits, needs no credit opt-in, and produces the identical actions from identical books — which is the only basis on which an owner can pre-approve a standing instruction at all. Quantities are read at fire time (the params hold *which item and why*, never a number), applying reuses Phase 31's executors and audit trail unchanged, and `approvalMode: 'auto'` is necessary but never sufficient: every action still passes Phase 31's category caps, and over-cap stays *held* rather than dropped or forced. Narrowly reverses Phase 31's "never log waste" decision — `inventory.waste.log` is `coworkerOnly`, so the model still cannot discover on its own that stock should go; only a job whose owner wrote down the item and the reason may. Adds «بازبینی حساب‌ها», twelve deterministic ledger checks with severities and suggested fixes (a rule engine, not a prompt — a plausible finding about money is worse than none), and opens the whole feature over `/api/v1` for sub apps |

| 33 | Phase-33-Assistant-Usability.md | Implemented — the assistant's usability, from reproduced complaints rather than speculation. On the surface: a cost estimate gated every single message behind an extra round trip and a tap (while `/api/ai/chat` was already doing its own credit reservation, so the gate bought nothing) — sending is now direct and the *actual* settled charge is shown quietly under the reply; two progress indicators ran at once, now one typing indicator; and Markdown was never rendered at all, so tables arrived as walls of `|---|` pipes and emphasis as literal asterisks — now rendered through a phone-first renderer where anything intrinsically wide scrolls inside its own box so the page never scrolls sideways. On substance: it asked owners for UUIDs (`find_items` resolves a partial Persian name), reported disabled items as "not found" (now reported as disabled, which is an answer), showed raw enum values like «spoilage» to Persian-speaking users (`ai-labels.ts` — a label the tool returns is a fact, one the model translates on the fly is a guess), could not answer "all bread waste ever" because only a current-stock tool existed (`get_waste_history`), and did not know which of five industries, which modules or which branches it was working inside (`describe_app`). Money now comes out of the tools as `{rial, toman, text}` rather than being the model's arithmetic |
| 34 | Phase-34-MCP-Connector.md | Implemented — every AI surface before this one pointed inward: the assistant, the digests, autopilot and the coworker all run a model *we* call, with *our* prompt, out of *our* credit balance. The owner who already pays for Claude or ChatGPT and wants to ask *it* about their café had nothing, and Phase 19's public API does not help — a chat client cannot read an OpenAPI document or mint itself a key. Adds an **MCP server** at `/api/mcp` (JSON-RPC over Streamable HTTP, no SSE because nothing is ever pushed) plus the OAuth 2.1 authorization server that makes "paste one URL and press connect" the whole of the setup: RFC 9728/8414 discovery reached from the 401's `WWW-Authenticate`, RFC 7591 dynamic client registration, S256-only PKCE, rotating refresh tokens. The load-bearing decisions: **read and write are separate grants** and, given write, **trust is a separate question again** — a connection is either `approve` (the write waits in the owner's list and changes nothing) or `apply`, chosen per connection and changeable without re-running OAuth on a phone; the **read tools are the assistant's own** (`toolDefinitions`/`runReadTool`, with one English sentence added in front of each Persian description) so the two surfaces cannot disagree about last week's revenue; and the **write tools are exactly the `ACTION_CATALOG` entries with a server-side executor**, opening no new mutation path — with `inventory.waste.log` absent and staying absent, because Phase 32 let a coworker job log waste only on the strength of a fact the owner wrote down in advance, and a model in a chat window has written down nothing. Three resources (`pos://app/overview`, `/conventions`, `/reports/catalog`) tell a model it is looking at integer Rial, Gregorian ISO dates and a trading day that is not a calendar day, before it guesses. Lives as a fourth tab on Phase 28's connections hub, with static `posmcp_…` tokens for header-only clients like Codex |
| 35 | Phase-35-Notifications.md | Implemented — everything the app knew about a till that came up 400,000 ﷼ short, a backup that failed at 03:00, or a coworker job waiting for approval since last night, it knew **on a screen nobody was looking at**. There was no mechanism at all: no table, no key pair, no service worker handler. Adds notifications over the **Web Push** standard (RFC 8030/8291/8292), which is what makes one implementation cover iOS, Android and Windows at once — implemented in `src/lib/web-push.ts` against Node's own crypto rather than as a dependency, the same precedent as `s3-lite.ts`, with a test that decrypts its own output using an independently written receiver. The iOS condition is the one that would have sunk it quietly: Safari does not expose `PushManager` outside a home-screen PWA, with no error and no prompt, so `pushSupport()` detects that case up front and answers with the instruction that fixes it. Producers only enqueue (`recordNotification` — one INSERT, error-swallowing, the same contract as Phase 32's `recordCoworkerEvent`, because a cashier closing their till must never wait on a push service in another country); a 15s tick claims each row and fans it out. **A missing rule is a default, not a silence** — otherwise the feature ships switched off on every install and the failure is invisible — and no event defaults a cashier or kitchen member into anything. **Quiet hours suppress the push and never the bell**: «بیدارم نکن» is not «به من نگو», with `backup.failed` the one `critical` event that overrides the window, which is its whole justification. Phase 32's jobs notify at their three outcome points, deterministically and with no model in the loop; notifications are deliberately **not** an `ACTION_CATALOG` action — they write nothing and need no approval, so an «ask me first» job would otherwise have to ask permission before telling anyone anything, and MCP's write catalogue is unchanged |
| 36 | Phase-36-App-Ecosystem.md | Implemented — the assistant had grown into four surfaces (chat, digests, autopilot, the coworker) plus MCP, all reached from a floating bubble in the corner of whatever page the owner was on: a thread had nowhere to live, the prompt was a constant that needed a deploy to change, and the model answered questions about the catalogue by *scanning* it, which works in the demo and fails at the customer. Adds a workspace shell behind a `workspace` flag, «از دستیار بپرس» reached from a page with that page's own context, **projects** (threads, notes and one standing instruction), prompt fragments as data where **the database row overrides the code default, not merely its version**, and `AppKey` grouping laid over `ModuleKey` gating — with `industry-profile.ts` still the only thing that gates, because a grouping that is the sole thing hiding a route is decoration. The load-bearing decisions are in the last two waves. **No number is ever copied into a vector**: `ai_embeddings` holds slow-moving text only, and orders, stock, payments and ledger rows are read through tools at the moment of asking, because a vector copy of a figure on a POS is stale within minutes and makes the model *confidently* wrong about money. **pgvector is optional**: migrations `0113`/`0114` create the extension inside a `DO … EXCEPTION` block and create nothing downstream unless it really landed, so the desktop installer's `embedded-postgres` loses RAG and not the assistant. And the semantic answer cache is keyed on the branch's **business day** rather than the calendar date — a café trading 18:00–03:00 runs one service, not two — plus a **tool signature** naming the tools and ranges the answer was built from, which is what makes a backdated order invalidate the window it landed in; only read-only turns are cached, the gate fails closed, and a cached answer is labelled with «دوباره بپرس» always forcing a fresh turn. Ten representative questions measured before and after: input tokens down 78%, credits down 73%, two previously wrong answers now right. Filed as 36 because 35 is taken by notifications; its GitHub issues (#358, waves #359–#366) were written before that phase landed and still say "Phase 35" |
| 36b | Phase-36b-Growth-Marketing-App.md | Implemented — the ecosystem's wave 1 grouped loyalty, campaigns/gift cards and commission under one app key, but they stayed three flat sidebar pages and the owner's question «بازاریابی‌ام چه خبر؟» had no one screen that answered it. This phase builds the app that key was promising: `/dashboard/growth`, with a management dashboard of its own («میز کار رشد» — KPIs over all four engines, leaderboards, a merged activity feed) and one section per engine, in the same rail shape the accounting suite set (`ledger-manager.tsx`). The three flat entries collapse into one «رشد و بازاریابی» and the old routes redirect into the app's sections. The load-bearing decisions: **no new tables** (the dashboard reads `promotion_applications`, `customer_points`, `gift_cards`, `commission_accruals` — tables the engines already own), **no new ledger writes** (issuing a gift card from the new section fires the same `promotions.gift_card_issued` event and posting rule; every KPI balance is reconstructed from `journal_lines` exactly as the trial balance does, so the dashboard cannot disagree with the books), and **the connection is made visible** rather than merely kept — a «پل حسابداری» card names the four accounts this app writes to (۲۳۰۰/۲۴۱۰/۲۴۲۰/۵۲۱۰) with their balances and a link into حسابداری. Adds campaign management the flat page never had: life-cycle states (inclusive date bounds exactly as the engine reads them — a campaign starting *today* is «در حال اجرا»), one-tap pause/resume through the existing upsert, and the effectiveness report beside the form; fixing along the way the old page's latent bug of rendering `p.name` from a payload that never carried one. A cashier lands on وفاداری and never sees commission or the KPIs, the same line the ledger's payroll tab draws. messaging (#372) grows this app rather than adding a sidebar peer; CRM (#367, row 36c below) and the website manager (#378) were both seated here as forward references and both later left for their own apps once built, for the same reason 36c gives for CRM |
| 36c | Phase-36c-CRM-App.md | Implemented — the third app, «ارتباط با مشتری», against GitHub issue #367. The customer record was the most-referenced entity in the product and the least-served: `customers` was touched by orders, loyalty, AR receipts, reservations, repairs, WooCommerce sync and the assistant, and the only screen that owned it was a flat table. Three failures drove it — a person was many rows (`0912…`, `۰۹۱۲…` and `+98912…` are one customer and three strings, so every per-customer number was wrong in a way nobody could see); consent was a boolean any screen could flip, which is not an answer when a customer asks who gave you permission to text them; and there was no way to ask «who should I call?» at all. Adds a peer app at `/dashboard/crm` with nine sections — dashboard, directory, a 360° customer file, segments, a deal pipeline, follow-ups, a service desk, duplicate detection with merge, and a consent register — plus canonical phone numbers, stored RFM/lifecycle scoring, four reports and four assistant read tools. **Departs from #367 twice, deliberately**: it is its own app rather than sections of Growth (the customer record is not a marketing engine, it is the thing five other apps point at — Growth keeps the audience engines, the CRM owns the record), and it includes the deal pipeline the issue put out of scope. The load-bearing decisions are all about not being trusted where it should not be: a segment is a JSON rule document compiled to parameterised SQL against a closed column union, never user SQL; consent is filtered *in the query* with a required `purpose` that fails closed, and «granted» is reported separately from «reachable» because a permission with no phone number promises an audience that cannot be delivered to; the timeline is a mapping over existing sources rather than a copied events table; and a merge is proposed, previewed, irreversible and never automatic, unioning tags but **intersecting** consent so it can never manufacture a permission neither record held. It is also the first app to add no posting rule and no ledger write at all: a deal is a forecast, not a document, and `crm.integration.test.ts` takes a full trial balance before and after a merge and demands they be identical. The assistant gets four read tools and exactly two writes (tag, note) — no path may change consent and merge is not an action, asserted in `ai.test.ts` by scanning every catalogue endpoint for `/consent` and `/merge` |
| 37 | Phase-37-LiteLLM-Gateway.md | Implemented — Introduces LiteLLM as the platform AI gateway: one OpenAI-compatible route, deploy-time model aliases and per-business virtual keys so tenant calls can be identified and revoked independently. The runtime resolves the credential and model alias once in `ai-runtime.ts`; routing, provider failover, load balancing and provider configuration remain LiteLLM policy, not app request fields. Billing remains Rial in the platform ledger, while LiteLLM spend is only diagnostic/input to settlement. The current technical console is `/platform/ai` (connection diagnostics plus virtual-key lifecycle), not a billing or model-market page. |
| 38 | Phase-38-WooCommerce-Catalogue-And-Store-Ops.md | Implemented — the catalogue a store actually has, and the operations an owner actually asks for, closing what issue #118 left open after Phase 23b. Five real failures: `wc/v3/products` never returns variations, so a variant shop's sellable rows simply did not exist; order lines ignored `variation_id` and resolved to the container parent, relieving no stock and posting no COGS; the webhook (`product_id` = parent + `variation_id` = child) and the plugin (`get_product()` ⇒ the variation in `product_id`) resolved one sale to two different rows; categories, tags, brands and `pa_*` attributes were discarded as free text; and Phase 23b's outbox could only set a stock number or a price. Adds `0120_woocommerce_catalogue_ops.sql` (`integration_woo_terms` + `integration_woo_product_terms`, both RLS-scoped), a type matrix where unknown types degrade to sellable rather than disappearing, `resolveWooOrderLine`'s `via` discriminator, and three new outbox operations — `product_update`, `order_status`, `refund_create` — all routed through the existing queue so they work in plugin mode where the app holds no store credentials at all. **Refunds never touch the gateway** (`api_refund` forced false twice); `PRODUCT_UPDATE_FIELDS` is a closed list of eight that refuses `type`/`parent_id`/`sku`. The cron is **split**, as chosen: three hooks (push/pull every 5 min, orders sweep 15 min, catalogue hourly), each independently scheduled, retryable and visible in WP admin, with exponential backoff on a new `available_at` column so one broken row no longer eats a slot in every batch. The plugin's `export_products()` is two passes with no `type` filter — 1.0.x silently dropped grouped, external and every extension type — parents before children, variations as their own events rather than an embedded array. Plus WP-CLI, a scheduling screen, and a reconciliation view. **Two things remain open**: `menu_items` has no parent/child concept, so what `variable`/`grouped` mean for a `food_service` business is still undecided and needs a schema change, and the taxonomy mirror is read-only |
| 38b | Phase-38b-LiteLLM-Platform.md | Implemented — Adds measured LiteLLM cost capture (`x-litellm-response-cost`) and usage/spend plumbing while preserving the central wallet/allowance billing path. Historical prompt/MCP experiments are now treated as LiteLLM-side/provider-side policy: core tenant chat sends only app-owned OpenAI function tools, and optional MCP or fallback settings must never break ordinary chat. Plan/Billing owns prices, allowances, wallets and revenue; LiteLLM owns upstream model/provider routing. |
| 39 | Phase-39-LiteLLM-Only-AI-Platform.md | Implemented — Retires direct-vendor providers so `AiProvider` is LiteLLM-only and makes tenant requests fail closed when a required business virtual key is missing rather than silently using the master key. The branch layer remains for virtual-key identity and diagnostics, while app-side business/branch model overrides, budgets, fallback chains and MCP management are ignored/retired in favor of LiteLLM and Plan/Billing ownership. |
| 40 | Phase-40-App-Ownership-And-WP-Manager.md | Implemented — makes cross-app ownership explicit: each app keeps its own system and only shares/synchronises the data another app needs for its workflow; Accounting A/R links into Growth's read-only customer projection while CRM remains the canonical customer-record owner; WordPress/WooCommerce becomes the standalone WP Manager app with its own connection, catalogue, order, customer, content, media and queue workflows; technical Connections remains a separate app, and old WooCommerce paths redirect into WP Manager |
| 41 | Phase-41-Shared-Party-Record.md | Implemented |
| 42 | Phase-42-Warehouse-Module.md | Implemented — reshapes the inventory workspace around the warehouse's own menu (لیست انبارها, موجودی انبار, انبارگردانی, ثبت رسید انبار/حواله, رسید و حواله‌های انبار) and adds the module's missing piece: the **warehouse document** (`0141` + `warehouse-document-service.ts`) — a posted in/out source document where a receipt (رسید) posts Debit 1300 / Credit 4900 at the line's qty × whole-Rial cost (settling open negative layers first, the 0019/0090 generalisation) and an issue (حواله) consumes through `consumeInventoryExact` and posts Debit 5900 / Credit 1300 at the exact cost of what left. Both accounts exist in every industry's COA template (`otherIncome`/`otherExpense` join `WELL_KNOWN_CODES`). Warehouses are branches: the new list/add sections read and write locations through `createBranch`'s one write path, and the per-warehouse stock level reads `v_inventory_valuation` so screen, list and balance sheet agree. No existing section is rebuilt — the count keeps its code and gains its warehouse name; purchases/waste/transfers keep their own lists |
| 42b | Phase-42b-Retail-Warehouse-Module.md | Implemented — the same warehouse module Phase 42 gave F&B, rebuilt on the RETAIL stock model (`items`/`item_stock`/`item_batches`) for «خرید و انبار» (/accounting/inventory): three groups in a SectionNav rail (انبارها: add + list; سند انبار: register + list; اقلام و عملیات: موجودی انبار، انبارگردانی، خرید، حواله بازگشت، گزارش) with `?tab=` deep links. The retail warehouse document (`0142` + `retail-warehouse-document-service.ts`) is a posted in/out source document: a receipt (رسید) upserts the named lot into `item_batches` (existing lot → qty added, cost re-averaged, expiry COALESCEd; new lot → `receiveBatch`) and rolls `item_stock` to the weighted-average SUM of its batches (the 0078 invariant, via the `rollItemStockToBatches` write extracted from `receiveBatch`), posting Debit `<industry>Inventory` / Credit 4900; an issue (حواله) relieves the named lot at its own cost and posts Debit 5900 / Credit `<industry>Inventory`. Serial/weight items and suppliers are refused, and there are no negative layers — short stock, missing lot and over-lot are all refused rather than settled. The three existing retail forms (purchase receiving, supplier return, low/dead stock) are extracted into sections unchanged; the F&B module is untouched |
| 43 | Phase-43-Periodic-Inventory-System-LIFO.md | Implemented — the costing setup step gains two choices: the **inventory system** (`inventory.costing.system`: perpetual — the only behaviour that existed before, still the default when the field is absent — vs classic **periodic/ادواری**) and **LIFO** as a third costing method next to FIFO and weighted average. Perpetual LIFO borrows the entire FIFO lot machinery and differs only in consumption order — `isLotBased(method)` and `lotConsumptionOrderClause(method)` in `inventory-costing.ts` centralise the decision, and `v_inventory_valuation` (`0144`) reads `IN ('fifo','lifo')`. Periodic is uncompromised classic: a received purchase posts only Debit **5105 خرید طی دوره** (new well-known code, backfilled) / Credit cash|bankClearing|AP with no stock movement; sales record revenue only (`deductForOrder` returns zero cost, so no COGS entry); and the **period close** (`periodic_closings` + `periodic-closing-service.ts` + `/api/inventory/periodic-closings` + the «بستن دوره (ادواری)» tab) values the entered count with the locked method — classic pooled weighted average, periodic FIFO (ending = newest layers) or periodic LIFO (ending = oldest layers), all in `periodic-valuation.ts` — and posts one immutable compound entry: Dr 5100 COGS = beginning + purchases − ending (side flips if negative), net 1300 restatement, Cr 5105 for the period's purchases so 5105 zeroes at every close. Perpetual-only instruments (counts, waste, production, transfers, warehouse documents, NRV, supplier returns, opening stock, Holoo stock import) refuse with `periodic_system_unsupported` (409) and their tabs hide for periodic businesses; customer returns refund without restock. `costingLocked` for periodic locks on the first received purchase or first close. Specific identification, standard costing, the retail method and the moving-vs-weighted-average naming clarification are deliberately deferred to separate issues |
| 38w | Phase-38-Website-Manager.md | **Website manager (مدیریت وب‌سایت)** against issues #378–#382; filed 38w because "Phase 38" names the WooCommerce catalogue phase. Waves 1–4 implemented: a `WebsiteAdapter` interface (`src/lib/website/adapter.ts`, integer Rial throughout, Markdown content, one error type, `draftPost` never publishes) with an in-memory mock and a Payload provider that wraps the existing encrypted Eshobe connection in `src/lib/cms/client.ts` (including bounded multipart featured-image upload); the content screen supports status/date list, Markdown/title/slug/excerpt/image edits, an explicit draft save versus publish button, and a safe server-side image boundary (type, 5 MB limit and magic signature). Migration `0136` extends `eshobe_cms_connections` rather than making a second credential store. The Connections hub is owner-only/test-before-save/key-never-returned; product/stock/price one-way sync uses `website_product_map` + `website_outbox`; and the assistant's post publishing action remains always-confirmed, with no executor/autopilot/MCP path. Rule added to CLAUDE.md: «سایت ویترین است؛ منبع حقیقت اینجاست» |
| 37b | Phase-37-Messaging.md | **SMS/email marketing (messaging, metered and posted)** against issues #372–#377. (Filed 37b because "Phase 37" names the unrelated LiteLLM Gateway phase.) Waves 1–4 engine implemented and unit-tested: platform message config/credits mirroring Phase 18's AI billing (migration `0133`, `messaging-billing.ts` + `messaging-billing-pure.ts`); templates/campaigns/outbox with a send-time recipient snapshot and a server tick (migration `0134`, `message-campaigns-service.ts`, `message-outbox-service.ts`); a Kavenegar/SMTP adapter seam with recorded-fixture tests (`messaging/provider.ts`, `providers/*`); and Wave 4's marketing-expense document posted once per campaign by the engine (`5600` already on every chart, `2455` added, `message-cost-posting.ts`). Docs + CLAUDE.md guardrails added. The owner/console UI, real-recipient preview, campaign project dimension and ledger-backed project-cost report are shipped. Wave 5 adds deterministic consent-safe birthday, three-month-inactivity and order-ready coworker events through the existing approval/cap path, plus ROI that only joins a campaign's dedicated promotion to its recorded completed sales; campaigns without one are explicitly not calculable |

Phases 0–11 built a single-business POS. Phases 12–17 turn it into a multi-business platform:
many businesses isolated in one deployment, teams with real permissions, several branches per
business, a super-user console, an accounting suite, and — as of Phase 17 — entitlements,
rate limits, and a continuously-proven tenant boundary that make it safe to run for paying
strangers. Phase 12 is the load-bearing one — everything after it depends on the tenancy
boundary being right. Phase 18 turns the AI assistant (built earlier, without its own phase
doc) from a per-business bring-your-own-key toggle into a platform-run, metered service:
one operator-owned provider connection, and per-business credits/subscriptions billed against
a priced catalogue. Phase 18b is the other half: growing what the assistant can actually read
and do — more tools and confirm-gated actions across every module, plus role-scoped variants —
shipped in waves after Phase 18's metering exists to bill them. Phase 19 opens the platform to
external, third-party integrations for the first time — scoped API keys and outbound webhooks
so a business can build (or commission) a "sub app" against its own data. Phase 18 and
18b are now complete, so Phase 19 starts with its independently reviewable API-key and
tenancy foundation before exposing any external data routes. Phase 21 is a different kind of
expansion from everything before it: rather than adding a capability to the existing café/
restaurant business, it turns the platform multi-industry — gold/jewelry, watch, and
accessories retail alongside the existing F&B shape, sharing one Core Accounting layer via a
new domain-event posting engine and a generalized item/variant/serial model — complete as of its
seventh wave, with all four industries selectable at signup. Phase 23 goes back to
the tenancy boundary Phase 12 established and gives it the one thing it lacked: a browser origin
per business. Until it, every tenant on a deployment shared one origin — and therefore one cookie
jar, one localStorage, one service worker — with only an app-applied path prefix between them.
Phase 25 finishes what Phase 21 started. Phase 21 made the platform multi-industry underneath —
charts of accounts, item models, posting rules — but the shell above it stayed café-shaped: the
industry column only ever *added* a page, so a jewellery shop still saw tables, a kitchen, a menu
and a sidebar reading «کافه و رستوران», and its sales went straight to the ledger with no invoice,
no customer and no history. Phase 25 makes the business type decide which modules exist, what they
are called and how a sale is written down, and gives the retail trades the sale document they never
had — reusing `orders`/`order_items` and Phase 21's own sell services rather than building a second
way to account for a sale. It is also the first phase to put the industry in the super-admin's
hands: until it, every business the console provisioned was silently a café.
Phase 24 is the first phase to treat security as its own subject rather than as a section inside
another phase's work. Phases 12 and 23 built the two boundaries that matter — the tenant row
boundary in Postgres and the browser origin boundary — and Phase 24 deliberately leaves both
alone, addressing instead everything around them that was never built: response headers, lockout
on the password realms, a second factor on every full-privilege account, encryption of data at
rest and of local backups, and a network posture where a café's box need not be reachable from
the internet at all. It also records, as a decision rather than an omission, why zero-knowledge
end-to-end encryption is not available to a product whose server has to produce a trial balance.

Phase 30 goes back to the chart of accounts itself — the one thing no phase had looked at whole
since Phase 7. Each industry's seed chart had been extended by whichever phase needed an account,
so the four retail templates never picked up the accounts a *cross-industry* feature added: a
jewellery or cosmetics shop could not run payroll or month-end depreciation at all, because
`ledger` is a core module for every trade but `5200`, `1510` and `5700` were only ever in F&B's
chart. It also adds the headings no chart had — discounts, the service charge, bank charges, a till
over/short, payroll withholdings, income tax, borrowings, drawings — and cheques (چک), which the
system had no representation of whatsoever: no accounts, no tables, no endorsement, in a market
where cheques are most of the trade credit.

Phase 41 is a correction rather than a feature: customers, suppliers and staff had become
three copies of the same idea — one table per app that needed a counterparty first, each with
its own add form, its own archived flag and its own name for the same shop. `parties` is now
the single record, `role` says which of the three a row is, and every app's party screen is a
scope over that one table (`src/lib/parties-scopes.ts`), including Accounting, which is the
only scope allowed to write a ledger code, a tax rate or a bank account. What did *not* change
is the vocabulary a person already knows: the CRM still says «مشتریان», the store still says
«تأمین‌کنندگان», and the assistant's tools still resolve a customer — the shared record is what
stops those screens from disagreeing, not a new thing for the owner to learn.

For overall architecture, full schema, and product summary, see the master spec doc (POS-Spec.md).
