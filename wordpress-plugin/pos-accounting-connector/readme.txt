=== POS Accounting Connector ===
Contributors: eshobe
Tags: woocommerce, accounting, pos, sync, erp
Requires at least: 6.0
Tested up to: 6.7
Requires PHP: 7.4
Stable tag: 1.6.4
License: GPLv2 or later
License URI: https://www.gnu.org/licenses/gpl-2.0.html

اتصال امن و دوطرفهٔ فروشگاه ووکامرس به سامانهٔ فروش و حسابداری.

== Description ==

این افزونه فروشگاه ووکامرس شما را به سامانهٔ حسابداری متصل می‌کند:

* **از فروشگاه به حسابداری:** سفارش، برگشت وجه، محصول (ساده، متغیر، تنوع،
  گروهی، خارجی و هر نوع افزونهٔ دیگر)، مشتری، دسته‌بندی‌ها و ویژگی‌ها.
* **از حسابداری به فروشگاه:** موجودی انبار، قیمت، ویرایش محصول، تغییر وضعیت
  سفارش و ثبت برگشت وجه.

پنج رویداد زمان‌بندی‌شدهٔ جداگانه (ارسال/دریافت سریع، بازخوانی سفارش‌ها،
بازخوانی کاتالوگ، و بازخوانی روزانهٔ مشتریان و محتوا) هر کدام با دورهٔ خودشان
اجرا می‌شوند، و همهٔ کارها از خط فرمان هم در دسترس است: `wp pos-connector sync`.

همهٔ ارتباط‌ها از سمت وردپرس آغاز می‌شود؛ سامانهٔ حسابداری هیچ‌گاه به فروشگاه شما
وصل نمی‌شود. بنابراین فروشگاهی که پشت فایروال است یا آدرس ثابت ندارد هم بدون
تنظیم اضافه کار می‌کند.

= امنیت =

هر درخواست علاوه بر توکن، دارای مهر زمانی، شمارهٔ یک‌بارمصرف و امضای
HMAC-SHA256 روی متن دقیق درخواست است. توکن فقط برای همین اتصال معتبر است و
هر زمان می‌توانید آن را از پنل حسابداری تعویض کنید.

== Installation ==

1. پوشهٔ افزونه را در `wp-content/plugins/` قرار دهید و آن را فعال کنید.
2. در پنل اشوبه به «تنظیمات ← اتصال‌های فنی ← وردپرس و ووکامرس» بروید، یک
   اتصال از نوع «افزونهٔ وردپرس» بسازید و توکن نمایش‌داده‌شده را کپی کنید.
3. در پیشخوان وردپرس، منوی «اشوبه» را باز کنید؛ راهنمای راه‌اندازی در بخش
   «اتصال» نمایش داده می‌شود. آدرس سامانه و توکن را وارد کنید،
   «فعال‌سازی محلی افزونه» را بزنید و ذخیره کنید.
4. «آزمایش اتصال از وردپرس» را بزنید.
5. در بخش «همگام‌سازی» دورهٔ هر بازخوانی را انتخاب کنید. برای اجرای
   دقیق، یک کرون واقعی تنظیم کنید:
   `*/5 * * * * wp --path=/path/to/site pos-connector sync`

== Frequently Asked Questions ==

= افزونه چگونه به‌روزرسانی می‌شود? =

افزونه در مخزن وردپرس نیست. منبع به‌روزرسانی یا گیت‌هاب همین پروژه است
(پیش‌فرض) یا سرور خودتان — که با تنظیم `POS_CONNECTOR_UPDATE_URL` در افزونه
انتخاب می‌شود. وردپرس خودش (دو بار در روز، روی چرخهٔ به‌روزرسانی‌های خودش)
بررسی می‌کند و نسخهٔ جدید را در «افزونه‌ها ← به‌روزرسانی‌های موجود» و «پیشخوان ←
به‌روزرسانی‌ها» نشان می‌دهد. نصب، همان به‌روزرسانی معمولی وردپرس است — با یک
کلیک، با به‌روزرسانی خودکار، یا با `wp plugin update pos-accounting-connector`.
بررسیِ دستی هم از صفحهٔ تنظیمات و با دستور `wp pos-connector check-update`
ممکن است.

بستهٔ نصبی هر نسخه با اکشن دستی «Build plugin zip» در گیت‌هاب ساخته می‌شود؛
خروجی آن (zip و در حالت سرورِ خودتان، update.json) را دانلود و روی هاست خودتان
قرار می‌دهید. راهنمای کامل: `docs/wordpress-plugin-updates.md`.

= همگام‌سازی هر چند وقت اجرا می‌شود? =

هر پنج دقیقه، از طریق WP-Cron. چون WP-Cron با بازدید سایت اجرا می‌شود، در
سایت‌های کم‌بازدید بهتر است یک cron واقعی روی `wp-cron.php` تنظیم کنید.

= اگر سامانهٔ حسابداری در دسترس نباشد چه می‌شود? =

سفارش‌ها در صف افزونه می‌مانند و در اولین اجرای موفق ارسال می‌شوند. هیچ سفارشی
از دست نمی‌رود.

== Changelog ==

= 1.6.4 =
* رویداد به‌روزرسانی پیوست‌ها اکنون شناسهٔ عملیات مربوط به ارسال از کتابخانهٔ رسانهٔ مرکزی را همراه دارد؛ فایلی که سامانهٔ حسابداری به فروشگاه ارسال می‌کند، پس از اتمام بارگذاری در همان کتابخانه هم به‌عنوان «همگام‌شده» ثبت می‌شود.

= 1.6.3 =
* بهبودها و رفع اشکال.

= 1.6.2 =
* حذف صفحهٔ قدیمی و بلااستفادهٔ تنظیمات زیرمنوی ووکامرس (از ۱.۶.۰ منوی مستقل «اشوبه» جای آن را گرفته بود)؛ کلاس تنظیمات اکنون فقط پردازش‌کنندهٔ فرم‌های همان پیشخوان است.
* به‌روزشدن راهنمای نصب و پیام‌های خط فرمان مطابق منوی واقعی «اشوبه» (به‌جای مسیر قدیمی «ووکامرس ← اتصال حسابداری»).
* بازگشت نتیجهٔ «بررسی به‌روزرسانی» به همان برگهٔ «به‌روزرسانی» به‌جای صفحهٔ وضعیت.

= 1.6.1 =
* بهبودها و رفع اشکال.

= 1.6.0 =
* بازطراحی کامل پیشخوان افزونه به‌صورت منوی مستقل «اشوبه» با بخش‌های وضعیت، اتصال، همگام‌سازی، صف، عیب‌یابی و به‌روزرسانی؛ این پیشخوان حتی وقتی ووکامرس غیرفعال است برای تنظیم و عیب‌یابی در دسترس می‌ماند.
* ارسال قابلیت‌ها، تله‌متری سلامت، تصویر کامل طبقه‌بندی‌های محصول و نشانگرهای پایان خروجی کامل برای کاتالوگ، سفارش‌ها، مشتریان و محتوا.
* احترام به توقف همگام‌سازی از پنل اشوبه: آزمایش اتصال و سلامت ادامه دارد، اما کارهای دستی/زمان‌بندی‌شده داده‌ای جابه‌جا نمی‌کنند و نتیجهٔ گمراه‌کنندهٔ موفقیت نشان داده نمی‌شود.
* ایمن‌سازی کارهای غیرقابل‌تکرار با شناسهٔ عملیات برای ثبت برگشت وجه، ایجاد نوشته و ایجاد رسانه؛ بستهٔ به‌روزرسانی خودمیزبان نیز با SHA-256 مانیفست بررسی می‌شود.

= 1.5.1 =
* همگام‌سازی محتوای وردپرس دیگر به گزینهٔ همگام‌سازی محصولات وابسته نیست؛
  ویرایش نوشته و برگه حتی وقتی کاتالوگ ووکامرس خاموش است به سامانه می‌رسد.
* ویرایش اطلاعات رسانه‌ها نیز ارسال می‌شود و حذف دائمی نوشته، برگه یا رسانه،
  ردیف قدیمی را از کتابخانهٔ سامانه پاک می‌کند (انتقال به زباله‌دان همچنان با
  وضعیت «زباله‌دان» قابل مشاهده است).

= 1.5.0 =
* **ساخت دستی بستهٔ نصبی در گیت‌هاب:** اکشن جدید «Build plugin zip» فقط با دستور شما
  اجرا می‌شود (نه با push و نه به‌طور خودکار). هر اجرا نسخه را یک پله بالا می‌برد
  (کوچک/بزرگ/عمده، پیش‌فرض کوچک)، فهرست تغییرات را در readme.txt می‌نویسد، بستهٔ
  تمیز zip را می‌سازد، نسخه را در مخزن ثبت و برچسب می‌زند و بسته را برای دانلود
  بارگذاری می‌کند.
* **سرور به‌روزرسانیِ خودتان:** اگر نشانی مانیفست `update.json` در افزونه تنظیم
  شده باشد، بررسی و نصب به‌روزرسانی از سرور خودتان انجام می‌شود، نه از گیت‌هاب —
  مناسب شبکه‌هایی که گیت‌هاب در آن‌ها کند، فیلتر یا قطع است. مانیفست را همان اکشن
  می‌سازد؛ کافی است آن را کنار zip روی هاست خودتان بگذارید. تا وقتی نشانی تنظیم
  نشده باشد، گیت‌هاب (انتشارها و برچسب‌ها) منبع پیش‌فرض می‌ماند.
* نشانی دانلود داخل مانیفست باید HTTPS و روی همان میزبانِ خود مانیفست باشد؛
  غیر از آن نادیده گرفته می‌شود.
* صفحهٔ تنظیمات منبع به‌روزرسانی و متن فارسیِ خطاهای بررسی را نشان می‌دهد و
  `wp pos-connector status` هم منبع را چاپ می‌کند.

= 1.4.0 =
* **سیستم به‌روزرسانی داخلی:** افزونه در مخزن وردپرس نیست و تا ۱٫۳٫۰ فقط با
  جایگزینی دستی فایل‌ها به‌روز می‌شد. حالا همان صفحه‌ای که به‌روزرسانی بقیهٔ
  افزونه‌ها را نشان می‌دهد («افزونه‌ها ← به‌روزرسانی‌های موجود» و «پیشخوان ←
  به‌روزرسانی‌ها») نسخهٔ جدید این افزونه را هم نشان می‌دهد و با یک کلیک، با
  به‌روزرسانی خودکار وردپرس، یا با `wp plugin update pos-accounting-connector`
  نصب می‌شود؛ جزئیات نسخه و یادداشت انتشار هم در همان پنجرهٔ استاندارد دیده
  می‌شود.
* **منبع نسخه‌ها گیت‌هاب است:** انتشارها و برچسب‌های همین پروژه. انتشار با فایل
  zip پیوست، تمیز نصب می‌شود؛ بدون آن، بستهٔ کامل مخزن دانلود و پوشهٔ افزونه از
  دل آن بیرون کشیده می‌شود — یعنی «برچسب زدن» به‌تنهایی برای انتشار کافی است.
* **هزینهٔ هر فروشگاه برای گیت‌هاب روزی دو درخواست است:** بررسی روی همان چرخهٔ
  به‌روزرسانی وردپرس (دو بار در روز) سوار شده و نتیجهٔ آن ۶ ساعت کش می‌شود؛
  خطای گذرا ۱۵ دقیقهٔ بعد دوباره بررسی می‌شود و آخرین جوابِ خوب را پاک نمی‌کند.
* **بررسی دستی:** دکمهٔ «بررسی به‌روزرسانی» در صفحهٔ تنظیمات، و `wp pos-connector
  check-update` از خط فرمان — که پاسخ را به همراه نسخهٔ نصب‌شده در `wp
  pos-connector status` هم نشان می‌دهد.
* به‌روزرسانی حتی وقتی ووکامرس خاموش است کار می‌کند؛ چون راه‌حلِ مشکلِ
  یک اتصالِ خراب، اغلب نسخهٔ بعدی همین افزونه است.

= 1.3.0 =
* **صادرات کامل، در همان اجرا:** هر اجرای همگام‌سازی دیگر فقط یک بستهٔ ۱۰۰تایی
  می‌فرستد؛ تا پایان بودجهٔ زمانی هر اجرا، بسته‌ها پشت‌سرهم ارسال می‌شوند و
  پس از دریافت کارها هم دوباره ارسال ادامه می‌یابد. پیش‌تر خروجی کاملِ یک
  فروشگاه متوسط با سرعت ۵۰ رویداد در هر ۵ دقیقه (۶۰۰ رویداد در ساعت)
  تخلیه می‌شد — همین بود که «برنامه فقط بخشی از داده‌ها را می‌آورد».
* **محصولات پیش‌نویس و خصوصی هم صادر می‌شوند:** «بازخوانی کل کاتالوگ» دیگر
  فقط محصولات «منتشرشده» را نمی‌فرستد و با خروجی حالت REST (که همهٔ وضعیت‌ها
  را می‌خواند) هم‌ارز شد.
* **تنوع‌ها دسته‌بندی خود را از والد می‌گیرند:** ووکامرس دسته و برچسب را روی
  محصول والد ثبت می‌کند؛ پیش‌تر هر تنوع با فهرست خالی می‌رسید و در ستون
  دسته‌بندیِ برنامه دیده نمی‌شد.
* هم‌راستا با سامانه: سفارش‌های پرداخت‌نشده (در انتظار پرداخت، در بررسی،
  لغوشده، ناموفق) دیگر در حسابداری به‌عنوان فروشِ کامل و پرداخت‌شده ثبت
  نمی‌شوند؛ با رسیدن به وضعیت پرداخت‌شده (در حال انجام/تکمیل) وارد می‌شوند.
* **تلاش دوبارهٔ رویداد ناموفق واقعاً کار می‌کند:** سامانه تلاش دوبارهٔ همان
  شناسهٔ تحویل را دیگر «تکراری» نمی‌شمارد (تا ۱٫۲٫۱ یک خطای گذرا یعنی از
  دست رفتن دائمی آن رویداد — سامانه این اصلاح را در نسخهٔ خودش اعمال کرده
  است).

= 1.2.1 =
* **رفع خطای مرگبار افزونه:** نسخهٔ ۱.۲.۰ با یک خطای نحوی در
  `class-pos-sync.php` منتشر شده بود؛ افزونه اصلاً بارگذاری نمی‌شد و سایت با
  خطای PHP بالا نمی‌آمد. هیچ همگام‌سازی‌ای در ۱.۲.۰ انجام نمی‌شد.
* **کارِ خراب دیگر کل اجرا را از کار نمی‌اندازد:** خطاهای PHP (نه فقط
  Exception) هنگام اعمال یک کار گرفته می‌شوند، بنابراین کارهای موفقِ پیش از آن
  حتماً به سامانه گزارش می‌شوند — پیش‌تر اجاره‌شان منقضی می‌شد و دوباره اعمال
  می‌شدند (برای «ثبت برگشت وجه» یعنی برگشت وجه تکراری).
* رفع خطای مرگبار وقتی محصول مادرِ یک تنوع حذف شده باشد.
* **صف بر اساس حجم بسته‌بندی می‌شود، نه تعداد ثابت:** یک برگهٔ بلند یا توضیح
  طولانی محصول می‌توانست بستهٔ ۵۰تایی را از سقف ۲ مگابایتی سامانه رد کند و
  همان بسته هر بار با خطای «حجم بیش از حد» رد شود و صف را ببندد.
* دکمه‌های «بازخوانی کل کاتالوگ» و «بازخوانی سفارش‌های اخیر» به صفحهٔ تنظیمات
  اضافه شدند؛ از ۱.۱.۰ فقط از خط فرمان در دسترس بودند.
* `wp pos-connector test` در حالت موفق دیگر با کد خطا خارج نمی‌شود، و
  `wp pos-connector export content` اضافه شد.
* زمان‌بندی بازخوانی روزانهٔ مشتریان و محتوا در صفحهٔ تنظیمات و خروجی
  `wp pos-connector status` نمایش داده می‌شود، و حذف افزونه هر دو را هم پاک
  می‌کند.


= 1.2.0 =
* **رفع مشکل ناقص‌بودن مشتریان:** پس از اتصال، سامانه به‌طور خودکار درخواست
  خروجی کامل مشتریان، محصولات و سفارش‌ها را می‌دهد — دیگر فقط مشتریان
  تازه‌ثبت‌نام پس از اتصال دیده نمی‌شوند.
* **بازخوانی روزانهٔ مشتریان:** مشتریانی که از طریق واردکننده یا مسیرهای غیر
  استاندارد ساخته شده‌اند هم در صف قرار می‌گیرند (نقش customer و کاربران دارای
  سفارش).
* **صف ایمن‌تر:** رویداد ناموفق با عقب‌گرد نمایی از سر صف کنار می‌رود تا یک
  رخداد خراب، صدها رخداد سالم پشت سر خود را متوقف نکند.
* **مدیریت محتوای وردپرس:** مشاهده و ویرایش نوشته‌ها و برگه‌ها و همگام‌سازی
  رسانه‌ها از اپ «مدیریت وردپرس»؛ تغییرات محتوا به‌صورت خودکار ارسال می‌شود.
* درخواست «خروجی کامل محتوا» از سامانه پاسخ داده می‌شود.

= 1.1.0 =
* سه رویداد زمان‌بندی‌شدهٔ جداگانه: همگام‌سازی سریع (۵ دقیقه)، بازخوانی
  سفارش‌ها (۱۵ دقیقه) و بازخوانی کاتالوگ (ساعتی) — هر کدام با دورهٔ قابل تنظیم.
* ارسال درخت دسته‌بندی: دسته‌ها، برچسب‌ها، ویژگی‌ها و طبقه‌بندی‌های سفارشی.
* ارسال شناسهٔ تنوع (`variation_id`) کنار شناسهٔ محصول در هر قلم سفارش، تا
  سفارشی که از وب‌هوک می‌رسد و سفارشی که افزونه می‌فرستد دقیقاً به یک کالا
  متصل شوند.
* ارسال کل کاتالوگ، نه فقط محصولات ساده و متغیر: گروهی، خارجی و هر نوعی که
  افزونهٔ دیگری ثبت کرده باشد.
* کارهای تازه از سامانهٔ حسابداری: ویرایش محصول، تغییر وضعیت سفارش، ثبت برگشت
  وجه (بدون بازگرداندن وجه از درگاه).
* قلاب‌های تازه برای ذخیره و حذف تنوع‌ها.
* بازگشت نمایی برای رویدادهای ناموفق، تا یک ردیفِ خراب هر بار تلاش نکند.
* دستورهای WP-CLI: `status`، `sync`، `test` و `export`.

= 1.0.2 =
* نخستین انتشار عمومی.
* پشتیبانی کامل از محصولات متغیر (variable) و تنوع‌ها (variations): همگام‌سازی
  محصول متغیر اکنون تنوع‌های فرزند را نیز شامل می‌شود.
* ارسال فیلدهای کامل محصول (نوع، ویژگی‌ها، تنوع‌ها، گروه‌بندی، تصاویر) به
  سامانهٔ حسابداری.
* رفع مشکل عدم دریافت محصول در سامانه برای فروشگاه‌های خرده‌فروشی (retail).

= 1.0.1 =
* رفع خطای «invalid_json» در «آزمایش اتصال» و همگام‌سازی: درخواست‌های بدون محتوا
  اکنون به‌صورت یک شیء JSON خالی ارسال می‌شوند.

= 1.0.0 =
* نخستین انتشار: اتصال امضاشده، همگام‌سازی دوطرفه، صف ارسال، گزارش رویدادها و
  مدیریت اتصال.
